{
  "name": "feishu-fund-assistant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Feishu bot that routes fund requests through modular skills.",
  "scripts": {
    "start": "node src/server.mjs",
    "check": "node --check src/server.mjs && node --check public/admin.js && node scripts/check-runtime-guards.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
